import SectionWrapper from "../components/layout/SectionWrapper";

export default function OffHours() {
  return (
    <SectionWrapper id="offhours">
      <h2 className="text-2xl font-semibold tracking-tight text-neutral md:text-3xl">
        Beyond the Screen
      </h2>
      <p className="mt-4 text-sm text-neutral/70">
        This section will show your interests, hobbies, and personality outside
        of coding, with layered images and storytelling later on.
      </p>
    </SectionWrapper>
  );
}